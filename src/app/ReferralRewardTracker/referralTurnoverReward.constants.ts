/** Turnover the referred user must complete before the referrer is rewarded. */
export const REFERRED_TURNOVER_THRESHOLD = 3000;

/** Bonus credited to the referrer once per referred user. */
export const REFERRER_BONUS_AMOUNT = 300;

/** Referrer must complete this multiplier on the credited bonus before withdrawal. */
export const REFERRER_TURNOVER_MULTIPLIER = 3;
